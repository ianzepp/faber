#!/usr/bin/env bash
# Install the current radix binary as ~/.local/bin/faber.
set -euo pipefail

cd "$(dirname "$0")/.."

if [[ ! -f radix/target/release/radix ]]; then
    echo "Error: radix/target/release/radix not found. Run 'bun run build:radix' first."
    exit 1
fi

mkdir -p ~/.local/bin
rm -f ~/.local/bin/faber
ln -s "$(pwd)/radix/target/release/radix" ~/.local/bin/faber
echo "Installed: ~/.local/bin/faber -> $(pwd)/radix/target/release/radix"

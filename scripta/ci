#!/usr/bin/env bash
# Full Rust validation gate for the Faber workspace.

set -euo pipefail

cd "$(dirname "$0")/.."

cargo fmt --all -- --check
cargo test --all
cargo clippy --all-targets --all-features -- -D warnings
cargo build --release -p faber
cargo build --release -p radix --bin radix

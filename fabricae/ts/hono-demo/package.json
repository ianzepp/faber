{
    "name": "hono-demo",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "build": "bun run build:faber && bun run build:export",
        "build:faber": "for f in fons/*.fab; do bun run ../../../fons/cli.ts compile \"$f\" -o \"dist/$(basename \"$f\" .fab).ts\"; done",
        "build:export": "echo '\n// Bun server export\nexport default { port: 3000, fetch: app.fetch };' >> dist/index.ts",
        "dev": "bun run build && bun run --watch dist/index.ts",
        "start": "bun run dist/index.ts",
        "clean": "rm -rf dist/*.ts"
    },
    "dependencies": {
        "hono": "^4.6.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.0.0"
    }
}

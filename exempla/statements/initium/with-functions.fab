// Entry point with functions defined outside
//
// Functions declared outside initium become module-level declarations.
// The initium block calls them as needed.

functio greet(textus name) -> textus {
    redde scriptum("Salve, {}!", name)
}

functio add(numerus a, numerus b) -> numerus {
    redde a + b
}

initium {
    scribe greet("Marcus")
    scribe "Sum:", add(3, 5)
}

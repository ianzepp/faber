# CLI Module Test - Config Commands
# Mounted at "config" by main.fab

ex "./config/server" importa * ut serverModule

@ descriptio "Configuration management"
@ imperia "server" ex serverModule
incipit {}

@ imperium "list"
functio list() -> vacuum {
    scribe "Listing configurations..."
}

@ imperium "show"
functio show(textus key) -> vacuum {
    scribe scriptum("Showing config: §", key)
}

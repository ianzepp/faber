# CLI Module Test - Server Config Commands
# Mounted at "config/server" by config.fab

@ descriptio "Server configuration"
incipit {}

@ imperium "set"
functio set(textus key, textus value) -> vacuum {
    scribe scriptum("Setting § = §", key, value)
}

@ imperium "get"
functio get(textus key) -> vacuum {
    scribe scriptum("Getting §", key)
}

@ imperium "list"
@ alias "ls"
functio serverList(si bivalens verbose ut v) -> vacuum {
    scribe "Listing server configs..."
}

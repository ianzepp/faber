# CLI Module Test - Main Entry Point
# Testing @ imperia module mounting per issue #157

ex "./commands/config" importa * ut configModule

@ cli "monk"
@ versio "1.0.0"
@ descriptio "Test CLI with module mounting"
@ imperia "config" ex configModule
incipit {}

@ imperium "status"
functio status(si bivalens verbose ut v) -> vacuum {
    scribe "Status check..."
}

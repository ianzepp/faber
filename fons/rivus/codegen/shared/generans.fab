# Shared Codegen - Generator Interface
#
# Common interface for all code generators (TsGenerator, GoGenerator, etc.).
# Defines the minimal methods needed by shared codegen handlers.

# Common generator interface for shared codegen
@ publica
pactum Generans {
    # Generate indentation string for current depth
    functio ind() -> textus

    # Increment indentation depth
    functio intraProfundum() -> vacuum

    # Decrement indentation depth
    functio exiProfundum() -> vacuum
}

# Nested itera ex loops

incipit {
    # Nested array iteration
    fixum rows = [1, 2, 3]
    fixum cols = ["A", "B", "C"]

    itera ex rows fixum row {
        itera ex cols fixum col {
            scribe row, col
        }
    }

    # Multiplication table
    itera ex 1..4 fixum i {
        itera ex 1..4 fixum j {
            scribe i, "*", j, "=", i * j
        }
    }

    # Nested ranges
    itera ex 0..3 fixum x {
        itera ex 0..3 fixum y {
            scribe x, y
        }
    }
}

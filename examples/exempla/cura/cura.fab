# Zig allocator scope syntax is covered by parser and driver tests.
# Active examples avoid allocator scopes because Zig codegen is not currently supported.

incipit {
    nota "cura allocator syntax is Zig-only"
}

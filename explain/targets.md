+++
term = "targets"
kind = "concept"
category = "package"
canonical = true
summary = "Compilation backends and capability notes shown by faber targets or radix targets."
syntax = "faber targets"
aliases = ["target compatibility", "backends"]
related = ["manifest", "cli", "⇢", "⇒"]
+++

Use `faber targets` for the live target list. Core syntax is cross-target where implemented, while some codegen features remain backend-specific.

```fab
functio salve() → textus {
    redde "salve"
}
```

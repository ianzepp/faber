+++
term = "ab pipeline"
kind = "legacy"
category = "collection"
canonical = false
summary = "Retired collection pipeline syntax."
syntax = "<retired>"
related = ["ab", "lista", "tabula", "prima", "ultima", "summa"]
canonical_term = "lista"
+++

`ab` was the former collection pipeline entry point. It is retired; use ordinary collection methods and closures instead. This is unrelated to loop `ab` in `itera ab`.

```fab
fixum _ first ← numbers.prima(3)
```

+++
term = "≤"
kind = "operator"
category = "comparison"
canonical = true
summary = "Checks whether the left expression is less than or equal to the right expression."
syntax = "<expression> ≤ <expression>"
examples = ["examples/exempla/proba/proba.fab"]
aliases = ["less or equal", "less than or equal"]
legacy = ["<="]
related = ["≥", "≡"]
+++

Use `≤` for inclusive upper-bound comparisons.

```fab
adfirma score ≤ 100
```

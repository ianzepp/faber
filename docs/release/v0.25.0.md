# Faber 0.25.0

This release captures the non-merge commits between v0.24.0 and v0.25.0.

## Highlights

- Migrate imports to § ex sectional syntax
- Add § ex sectional import syntax (#218)
- Disable fit/fiet/fiunt/fient return syntax, fix issues #226-228
- Updated nihil/nonnihil errors in rivus
- Remove in mutation block syntax (closes #222)
- Fix parameter alias (ut) not registering in scope (#225)
- Fix ego to return instance type instead of metatype (#224)
- Fix destructuring patterns not registering bindings in scope (#221)
- Fix sed regex flag parsing to use inline syntax (#223)
- Extract shared iteratio codegen for range loops (#119)
- Extract shared elige codegen, unify Go to if/else chains (#119)
- Extract shared codegen logic for si and dum handlers (#119)
- Add --strip-tests flag to faber CLI
- Add nihil/nonnihil unary operators and --no-typecheck build flag
- Remove bun run faber/rivus, use opus/bin/* binaries directly
- Make rivus the primary compiler in documentation
- Update AGENTS.md
- Update agent command syntax in AGENTS.md
- Update AGENTS.md project layout to match actual structure
- Fix documentation errors: discerne syntax, stale references
- Rename build:exempla to exempla, add compiler flags
- Add --faber/--rivus/--artifex flags to build script
- Fix failing tests: todo stale local imports, update pro→clausura
- Add compiler warning for == nihil / != nihil patterns
- Consolidate rivus body parsing into parseCorpus helpers
- Add tacet keyword support to rivus compiler
- Add tacet keyword for explicit empty blocks (faber only)
- Flatten deeply nested conditionals in rivus semantic/codegen
- Complete lambda keyword refactor from 'pro' to 'clausura'
- Moved consilia out of the project entirely.
- Moved coreutils out of the project entirely.
- Moved zed grammar out of the project entirely.
- Moved archivum out of the project entirely
- WIP: refactor lambda keyword from 'pro' to 'clausura'
- feat(parser): store § section annotations on Programma AST
- feat(lexer): support triple-quote multi-line string literals
- fix(rivus): disambiguate keywords from function calls via LPAREN lookahead
- fix(faber): preserve namespace kind for HAL pactum imports
- refactor(cli): use figendum for async variable declarations
- feat(test): standalone test runner with annotation-based configuration
- refactor: simplify scribe/vide/mone to use implicit scriptum
- feat(rivus): auto-wrap string literals in scribe as scriptum
- feat(parser): auto-wrap string literals in scribe as scriptum

## Notes

- Tag range: v0.24.0..v0.25.0
- Generated from commit summaries in the reference history.

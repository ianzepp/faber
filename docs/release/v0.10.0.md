# Faber 0.10.0

This release captures the non-merge commits between v0.9.0 and v0.10.0.

## Highlights

- Complete bootstrap parser: add remaining statement parsers
- Complete object literal and lambda expression parsers in bootstrap
- Replaced /// with #
- Remove inline futura/cursor modifiers, use @ annotations only
- Add generis examples to publica.md with proper Latin adjective order
- Support @ futura, @ cursor, @ generis annotations for async/generator/static
- Default visibility to private, require @ publicum for exports
- Update docs to use @ annotation syntax for modifiers
- Implement @ annotations for modifiers (visibility, abstract, async)
- Replaced // with # in exempla
- Simplify CommentType to 'line' only
- Remove // and /* */ comments, use # only
- Add # as line comment indicator
- Use @ for annotations, # for comments in notae design doc
- Add BUG comment for missing longitudo -> length translation
- Add design doc for publicus module exports
- Use -> instead of fit for sync functions in bootstrap
- Wire up Parsator to complete parser mutual recursion
- Remove aliter keyword, use secus for else clauses
- Add statement and declaration parsers to bootstrap compiler
- Remove sin/aliter/secus from statementKeywords blocklist
- Allow cape/demum as identifiers in expression context
- Update bootstrap-ts.md: AST restructure complete
- Restructure AST as discretio variants
- Remove keyword conflicts gotcha - keywords now allowed as identifiers
- Allow keywords as variable names

## Notes

- Tag range: v0.9.0..v0.10.0
- Generated from commit summaries in the reference history.

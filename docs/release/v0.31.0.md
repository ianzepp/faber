# Faber 0.31.0

This release captures the non-merge commits between v0.30.0 and v0.31.0.

## Highlights

- fix(radix): improve unnecessary cast warning to handle aliases and skip construction
- fix(radix): correctness fixes for Conversio codegen and resolve
- feat(radix): promote conversio to ⇒ glyph with dedicated HIR node (#414)
- feat(radix): promote qua/innatum/novum to unified ⇢ glyph (#411)
- fix(radix): correctness fixes for Verte lowering and codegen
- feat(radix): unify qua/innatum/novum into HirExprKind::Verte (#413)
- Promote bitwise and shift operators to Unicode glyphs
- Promote compound assignment to Unicode glyph operators
- Migrate range syntax to Unicode glyphs (‥/…)
- docs: archive obsolete faber-ts reference implementation
- test(ts): add radix TypeScript integration coverage (#403)
- codegen(ts): translate norma methods and intrinsic calls (#402)
- codegen(ts): implement expression emission backend (#401)
- codegen(ts): generate declarations and statements (#400)
- codegen(ts): scaffold radix TypeScript target and type mapping (#399)
- feat(radix): add regex literal type through lowering and codegen
- migrate(rivus): switch self-hosted compiler sources to Unicode operators (#396)
- nanus-rs: add Unicode operator support across pipeline (#395)
- golden: update Faber snapshots for Unicode operators (#394)
- migrate(proba): convert YAML Faber snippets to Unicode operators (#393)
- migrate(exempla): switch .fab examples to Unicode operators (#392)
- codegen(radix): emit Unicode operators in Faber backend (#391)
- parser(radix): align operator parsing with Unicode core syntax (#390)
- lexer(radix): add Unicode operator tokenization (#389)
- spec: adopt Unicode core operators in EBNF (#388)
- fix(radix): lower faber-path control flow without error nodes
- fix(radix): preserve object fields in faber innatum output
- fix(radix): avoid nested casu blocks in faber output
- fix(radix): emit canonical si/sin/secus and reddit
- fix(radix): preserve parameter names in faber codegen
- refactor(radix): unify primitive and collection type helpers
- refactor(radix): extract lexer emit/operator/radix helpers
- fix(build): use writable temp path for rustc verify and add error summary
- fix(radix): address rust codegen strings parens and imports
- fix(radix): propagate codegen errors instead of dropping
- fix(radix): guard direct faber codegen against HIR errors
- fix(radix): fail fast on HIR error expressions
- fix(radix): reject unsupported incipit argumenta lowering
- Allow codegen when semantic diagnostics are warnings only
- Fix mutable ex destructuring assignment type checks
- Fix ab inference through object-member collection sources
- Fix remaining exempla long-tail failures in radix
- Support externa declarations and duplicate import module bindings
- Fix method-call condition typing and stmt inference fallback
- fix(radix): infer empty arrays and relax lista literal compatibility
- fix(radix): reduce borrow false positives for normal flow
- feat: add rust-correctness-surgeon agent for radix-rs
- fix(radix): preserve numeric flow for destructures and method returns
- fix(radix): support optional params in call arity checks
- fix(radix): allow enum variants in elige case values
- fix(exempla): correct curator param to curata modifier in validatio.fab
- fix(radix): support qua, innatum, and vel expression flows
- fix(radix): implement collection pipeline lowering (#344)
- fix(radix): implement optional-chain lowering (#342)
- fix(radix): resolve issue #345 unknown type forms
- fix(radix): correct lvalue typing for ego fields and indices
- fix(radix): lower proba statements into test functions
- fix(radix): support missing binary and unary operators
- fix(radix): improve expression and variable inference
- fix(radix): support member access beyond struct-only checks
- fix(radix): implement scriptum interpolation lowering
- fix(radix): resolve unknown identifier false positives
- fix(radix): reduce type mismatch false positives in checker
- refactor(radix): cargo fmt
- refactor: replace build script with radix-rs focused pipeline
- docs(radix): comprehensive code documentation pass across all modules
- refactor(radix): cargo fmt + resolve all clippy warnings
- feat: add technical-writer agent for code documentation passes
- Resolve #332 #333 #334: failable iace pipeline in radix-rs
- Add ignored Rust exempla end-to-end harness
- Add Rust assert/panic lowering and usage-driven imports
- Implement Rust main/print codegen and finish decl/expr gaps
- Resolve Rust codegen identifier placeholders
- test(radix): add safe CLI path coverage in main
- test(radix): add source file utility coverage
- test(radix): cover session config and compiler entrypoints
- test(radix): add driver pipeline coverage for compile flow
- test(radix): raise rust expr/types coverage with focused unit cases
- test(radix): add Rust codegen coverage for context wiring
- feat(radix): wire name resolution context into Rust codegen
- refactor(radix): simplify RustOutput to code-only
- feat(radix-rs): add permissive mode for semantic check (#331)
- feat(radix-rs): PR A - HIR lowering coverage cleanup for #316 (#330)
- docs: update README.md and AGENTS.md to reflect current project state
- feat(radix-rs): tighten ignotum handling and unresolved type output (#317)
- feat(radix-rs): enforce Unicode XID identifiers and NFC interning (#315)
- feat(radix-rs): lint de/in/ex mode misuse in borrow analysis (#314)

## Notes

- Tag range: v0.30.0..v0.31.0
- Generated from commit summaries in the reference history.

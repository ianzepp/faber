# Faber 0.7.0

This release captures the non-merge commits between v0.6.0 and v0.7.0.

## Highlights

- Release v0.7.0
- Removed old rivus contents
- Add lege() stdin codegen for TypeScript
- Update bootstrap doc: Phase 3 I/O complete via stdin/stdout
- Add lege() stdin expression, disable unimplemented cura curator kinds
- Fix Zig output streams: scribe->stdout, mone->stderr, vide->debug
- Add confla/inLista to Zig tabula, update bootstrap doc with recent progress
- Remove JS-style arrow syntax, require colon in lambda expressions
- Zig codegen: fix lambda type inference and Lista construction
- Add wrap property to test runner for allocator context

## Notes

- Tag range: v0.6.0..v0.7.0
- Generated from commit summaries in the reference history.

# Faber 0.11.0

This release captures the non-merge commits between v0.10.0 and v0.11.0.

## Highlights

- Fix bootstrap compiler: exports, enum names, and stdlib methods
- Update AGENTS.md
- Add codegen entry point and CLI for bootstrap
- Use si optional params in fons-fab where appropriate
- Moved CLAUDE.md to AGENTS.md
- Recommend ut over pro for discerne bindings
- Document parameter modifiers (si/vel) in CLAUDE.md
- Add optional parameters with si/vel syntax
- Fix discretio return type mismatch for finge...qua expressions
- Added test doc files
- Create si-intra.md
- Refactor fons-fab to use scriptum() instead of string concatenation
- Refactor fons-fab codegen to use ut alias binding in discerne
- Regenerate grammar docs (lege lineam syntax)
- Add grammar verification guidelines to AGENTS.md
- Update bootstrap-ts.md progress tracking
- Remove unnecessary qua casts in fons-fab semantic files
- Add bootstrap TypeScript codegen in Faber (WIP)
- Disable format command (prettier plugin archived)
- Fix scriptum() brace escaping in TS codegen
- Add ut alias binding syntax to discerne pattern matching
- Renamed disabld prettier test
- Fix discerne pattern binding type inference
- Add lineam keyword to fons-fab lexicon
- Update bootstrap-ts.md
- Update lege to drop parens, add lege lineam for single-line stdin
- Move prettier out of fons/
- Add semantic analyzer for bootstrap compiler (Phase 2)
- Add cross-model LLM validation finding to README
- Extract sync keywords to lexicon/verba.fab as single source of truth
- Fix parser idiom inconsistencies and add LLM readability goal
- Update bootstrap-ts.md: reflect parser completion (27 files, 6,259 lines)

## Notes

- Tag range: v0.10.0..v0.11.0
- Generated from commit summaries in the reference history.
